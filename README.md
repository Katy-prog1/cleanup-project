# 🧹 Bash Cleanup Project

A simple Bash project to clean up temporary and log files from `/tmp`, `/var/tmp`, and `/var/log`.

## Files

- `cleanup.sh` — deletes old files.
- `crontab.sh` — adds a weekly cron job.
- `menu.sh` — interactive script menu.
- `bootstrap.sh` — sets up the environment.

## Usage

```bash
chmod +x *.sh
./menu.sh
